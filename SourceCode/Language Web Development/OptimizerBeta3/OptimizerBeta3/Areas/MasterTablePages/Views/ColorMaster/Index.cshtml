@model IEnumerable<OptimizerBeta3.Models.MasterTables.ColorMaster>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="border backgroudColor">
    <div class="row">
        <div class="col-6">
            <h2 class="text-info">Color Master - Index</h2>
        </div>
        <div class="col-6 text-right">
            <partial name="_CreateButtonPartialView" />
        </div>
    </div>
    <div></div>
    <br />

    <div>
        @if (Model.Count() > 0)
        {
            <table class="table table-striped border">
                <tr class="table-secondary">
                    <th>
                        @Html.DisplayNameFor(m => m.FKColour)
                    </th>
                    <th>
                        @Html.DisplayNameFor(m => m.ColourName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(m => m.Combination)
                    </th>
                    <th><label>Actions</label></th>
                </tr>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(m => item.LookUpMasterColour.Description)
                        </td>
                        <td>
                            @Html.DisplayFor(m => item.ColourName)
                        </td>
                        <td>
                            @Html.DisplayFor(m => item.Combination)
                        </td>
                        <td style="width:150px">
                            <partial name="_TableButtonPartial" model="item.Id" />
                        </td>
                    </tr>
                }
            </table>
        }
        else
        {
            <p> No Company Info exists.....</p>
        }
    </div>
</div>
