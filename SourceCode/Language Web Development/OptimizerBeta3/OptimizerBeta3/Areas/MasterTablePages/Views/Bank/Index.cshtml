@model IEnumerable<OptimizerBeta3.Models.MasterTables.Bank>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="border backgroudColor">
    <div class="row">
        <div class="col-6">
            <h2 class="text-info">Bank's - Index</h2>
        </div>
        <div class="col-6 text-right">
            <partial name="_CreateButtonPartialView" />
        </div>
    </div>
    <div></div>
    <br />

    <div>
        @if (Model.Count() > 0)
        {
            <table class="table table-striped border">
                <tr class="table-secondary">
                    <th>
                        @Html.DisplayNameFor(m => m.FKBankName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(m => m.BranchName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(m => m.BranchCode)
                    </th>
                    <th>
                        @Html.DisplayNameFor(m => m.IFSCCode)
                    </th>
                    <th class="text-center"><label>Actions</label></th>
                </tr>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(m => item.LookUpMasterBank.Description)
                        </td>
                        <td>
                            @Html.DisplayFor(m => item.BranchName)
                        </td>
                        <td>
                            @Html.DisplayFor(m => item.BranchCode)
                        </td>
                        <td>
                            @Html.DisplayFor(m => item.IFSCCode)
                        </td>
                        <td style="width:150px" class="text-center">
                            <partial name="_TableButtonPartial" model="item.Id" />
                        </td>
                    </tr>
                }
            </table>
        }
        else
        {
            <p> No Company Info exists.....</p>
        }
    </div>
</div>
